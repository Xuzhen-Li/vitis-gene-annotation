# Overview

Single map: [`docs/FULL_PIPELINE.md`](../docs/FULL_PIPELINE.md).  
Peer table: [`docs/PEER_PIPELINES.md`](../docs/PEER_PIPELINES.md).

Default path: soft-mask → RNA → BRAKER3 + GeMoMa → EVM merge → AGAT → BUSCO/PSAURON → GSAman (priority) → release.
