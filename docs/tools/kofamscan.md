# KofamScan — KEGG KO on proteins

**Role:** F1 optional / complements eggNOG KEGG columns.

## Get it
KEGG KofamKOALA downloads: profiles + `ko_list` + `exec_annotation`.

## Run
`pipeline/F1b_kofam.sh` with `KOFAM_PROFILE_DIR`, `KOFAM_KO_LIST`, `RUN=1`.
