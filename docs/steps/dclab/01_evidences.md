# S14 / 01 — External evidences

```bash
# Iso-Seq HQ and/or related-species CDS / RNA assemblies — all FASTA
cp /path/to/isoseq_HQ.fasta "${EXTERNAL_DIR}/"
cp /path/to/related.CDS.fasta "${EXTERNAL_DIR}/"
```

Used in PASA training (03) and polishing (07).
